# Бюджетный помощник

Командный проект по дисциплине «Основы программирования».

Выполнили: Бадамшина Алина Мидхатовна 502831, Иванова Екатерина Алексеевна 501171

Тема: **Бюджетный помощник**.

-----

## Структура проекта

```
budget_assistant/
main.py            # Точка входа, меню пользователя
budget.py          # Основная логика (BudgetAssistant)
data_structures.py # Стек и дерево BST
README.md          # Документация
```

-----

## Запуск

```bash
# Запуск программы
python main.py

# Запуск тестов
python tests.py -v
```

-----

## Реализованные алгоритмы и структуры данных

### 1. Префиксные суммы — запрос суммы за период O(1)

Массив `prefix[i]` хранит сумму расходов с 1-го по i-й день.  
Запрос суммы за период [A, B]:

```
prefix[B] - prefix[A-1]
```

Сложность: **O(1)** на запрос, **O(n)** на построение.

-----

### 2. Линейный поиск дня с максимальным расходом

Проходим по всем дням и сравниваем суммы.  
Сложность: **O(n)**.

-----

### 3. Сортировка вставками категорий по сумме трат

```python
for i in range(1, len(items)):
    key = items[i]
    j = i - 1
    while j >= 0 and items[j][1] < key[1]:
        items[j + 1] = items[j]
        j -= 1
    items[j + 1] = key
```

Сложность: **O(n²)**.

-----

### 4. Стек для отмены последнего расхода

При добавлении расход кладётся на стек (`push`).  
При отмене — извлекается (`pop`) и удаляется из данных.

-----

### 5. Дерево BST для хранения расходов

Бинарное дерево поиска по сумме расхода.  
Обход `inorder` даёт расходы в порядке возрастания суммы.

-----

## Функции программы

|Пункт меню|Функция                                       |
|----------|----------------------------------------------|
|1         |Добавить расход (день, сумма, категория)      |
|2         |Отменить последний расход (стек)              |
|3         |Сумма расходов за период [A, B]               |
|4         |День с максимальными расходами                |
|5         |Категории по сумме трат (сортировка вставками)|
|6         |Дерево расходов (BST, inorder)                |
|7         |Все расходы по дням                           |

-----

## Распределение функционала (пример для команды 2 человека)

|Участник  |Функционал                                                                    |
|----------|------------------------------------------------------------------------------|
| Бадамшина Алина  |`data_structures.py`: Stack, ExpenseTree; функции `add_expense`, `undo`       |
| Иванова Екатерина|`budget.py`: префиксные суммы, линейный поиск, сортировка вставками; `main.py`|
